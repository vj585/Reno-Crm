{
  "name": "xeno-ai-campaign-copilot",
  "version": "1.0.0",
  "description": "AI-native Mini CRM",
  "scripts": {
    "start": "concurrently \"npm run dev:backend\" \"npm run dev:channel\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:channel": "cd channel-service && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../channel-service && npm install && cd ../frontend && npm install",
    "seed": "cd backend && npm run seed"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
